/node_modules
/.next
/out
/.vercel
*.tsbuildinfo
next-env.d.ts
.env*.local
.DS_Store
/_tgzs
/_nmcache
lint_full.txt

# lock 文件不入库（避免跨平台原生二进制缺失导致 CI 构建失败）
package-lock.json

# Local development secrets (never commit)
.dev.vars
.dev.vars.*

# E2E 运行产物（下载的 ZIP / 解压树 / 运行日志），不入库
e2e-out/
